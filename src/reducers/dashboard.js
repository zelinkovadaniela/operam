import campaign from '../campaign';

const initialState = {
  campaign,
};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
